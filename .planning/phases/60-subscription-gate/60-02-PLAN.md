---
phase: 60-subscription-gate
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - tests/integration/subscription-gate.test.ts
  - tests/e2e/subscription-gate.spec.ts
autonomous: false
requirements: [REQ-05]

must_haves:
  truths:
    - "Integration-Tests beweisen Gate-Logik mit echten DB-Queries"
    - "E2E-Tests beweisen User-Flow vom Gate bis zum Upgrade"
    - "Alle bestehenden Tests laufen weiter ohne Regression"
  artifacts:
    - path: "tests/integration/subscription-gate.test.ts"
      provides: "Integration-Tests fuer Gate + Upgrade"
      min_lines: 80
    - path: "tests/e2e/subscription-gate.spec.ts"
      provides: "E2E-Tests fuer Subscription-Gate-Flow"
      min_lines: 50
  key_links:
    - from: "tests/integration/subscription-gate.test.ts"
      to: "lib/subscription/queries.ts"
      via: "import getSchoolSubscriptionTier, isGated"
      pattern: "getSchoolSubscriptionTier|isGated"
    - from: "tests/e2e/subscription-gate.spec.ts"
      to: "/kind/ueben"
      via: "Playwright navigation"
      pattern: "kind/ueben|kind/upgrade"
---

<objective>
Integration- und E2E-Tests fuer das Subscription-Gate schreiben, plus visuelle Verifikation.

Purpose: Beweist dass Gate, Upgrade, und Demo-Bypass korrekt funktionieren.
Output: Vollstaendige Testabdeckung fuer Phase 60 Success Criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-subscription-gate/60-CONTEXT.md
@.planning/phases/60-subscription-gate/60-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 60-01 erstellte Exports: -->
From lib/subscription/queries.ts:
```typescript
export type SubscriptionTier = "free" | "grundschule" | "foerderung" | "experte" | "demo";
export async function getSchoolSubscriptionTier(supabase: SupabaseClient<Database>, userId: string): Promise<SubscriptionTier>;
export function isGated(gradeLevel: number, tier: SubscriptionTier): boolean;
```

From app/(child)/kind/upgrade/actions.ts:
```typescript
export async function upgradeSubscriptionAction(formData: FormData): Promise<void>;
// formData.get("tier") muss in ["grundschule", "foerderung", "experte"] sein
```

<!-- Bestehende Test-Infrastruktur: -->
From tests/fixtures/supabase.ts:
```typescript
export function adminClient(): SupabaseClient<Database>;
export const TEST_SCHOOL_NAME: string;  // "Testschule Alpha"
export const TEST_CLASS_NAME: string;   // "Klasse Test-1a"
export const TEST_TEACHER: { email, password, name };
export const TEST_CHILD: { username, pin, grade: 2 };  // Klasse 2!
export async function seedTestData(): Promise<SeedResult>;
export async function cleanupTestData(): Promise<void>;
```

<!-- DB CHECK-Constraint nach Migration: -->
subscription_tier IN ('free', 'grundschule', 'foerderung', 'experte', 'demo')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration-Tests (Gate-Logik + Upgrade-Action + RLS)</name>
  <read_first>
    - tests/integration/exercise-actions.test.ts (bestehendes Integration-Test-Pattern)
    - tests/integration/start-game-action.test.ts (Server Action Test-Pattern)
    - tests/fixtures/supabase.ts (Seed-/Cleanup-Pattern)
    - lib/subscription/queries.ts (zu testende Funktionen)
    - supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql (neue RLS-Policies)
  </read_first>
  <files>tests/integration/subscription-gate.test.ts</files>
  <action>
Integration-Tests in `tests/integration/subscription-gate.test.ts` erstellen. Pattern aus bestehenden Tests uebernehmen (adminClient fuer Setup, user-context Client fuer RLS-Pruefung).

**Benoetigte Fixture-Daten:** Da TEST_CHILD.grade = 2 (Klasse 2), brauchen wir ein separates Kind in Klasse 4 fuer die Gate-Tests. Eigene Seed-Logik innerhalb des Tests (wie in start-game-action.test.ts).

Konstanten:
```typescript
const SUB_TEST_SCHOOL = "Sub-Gate Testschule";
const SUB_TEST_CLASS = "Sub-Gate Klasse 4a";
```

**Tests:**

1. **`isGated` pure function tests** (kein DB-Zugriff):
   - `isGated(4, 'free')` returns true
   - `isGated(4, 'grundschule')` returns false (bezahlt)
   - `isGated(4, 'foerderung')` returns false
   - `isGated(4, 'experte')` returns false
   - `isGated(4, 'demo')` returns false (per D-12)
   - `isGated(3, 'free')` returns false (per D-02)
   - `isGated(1, 'free')` returns false
   - `isGated(2, 'free')` returns false

2. **`getSchoolSubscriptionTier` mit DB** (braucht laufenden Supabase):
   - Setup: Schule mit tier='free' + Klasse + Kind-Profil in Klasse 4
   - Query gibt 'free' zurueck
   - Schule auf 'grundschule' updaten (admin) -> Query gibt 'grundschule' zurueck
   - Schule auf 'demo' updaten (admin) -> Query gibt 'demo' zurueck

3. **RLS-Tests**:
   - Kind kann subscription_tier seiner eigenen Schule lesen (child_reads_own_school)
   - Kind kann subscription_tier seiner eigenen Schule updaten (child_upgrades_own_school)
   - Kind kann NICHT eine fremde Schule lesen (RLS blockiert)

4. **Upgrade-Flow**:
   - Schule hat tier='free' -> Kind setzt tier='grundschule' via Supabase update (simuliert Server Action) -> tier ist 'grundschule'
   - Ungueltiger tier-Wert ('paid', 'invalid') wird von CHECK-Constraint abgelehnt

Cleanup: Alle Test-Daten nach jedem Test aufraeumen.

Nutze `describe`/`it` Pattern mit `beforeAll`/`afterAll` fuer Seed/Cleanup. Pruefe mit `isServerUp()` Pattern aus bestehenden Tests ob Supabase laeuft, und skippe anderenfalls.
  </action>
  <acceptance_criteria>
    - grep -q "isGated" tests/integration/subscription-gate.test.ts
    - grep -q "getSchoolSubscriptionTier" tests/integration/subscription-gate.test.ts
    - grep -q "demo" tests/integration/subscription-gate.test.ts
    - npx vitest run tests/integration/subscription-gate.test.ts (alle Tests bestehen)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ozguroztopcu/Desktop/KI-Projekte/Project\ Antigravitiy/Matheapp && npx vitest run tests/integration/subscription-gate.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - isGated-Tests beweisen: Klasse 1-3 nie blockiert, Klasse 4 nur bei 'free' blockiert, demo durchgelassen
    - getSchoolSubscriptionTier-Tests beweisen korrekten DB-Pfad
    - RLS-Tests beweisen Datenisolation
    - Alle Tests bestehen
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E-Tests (Gate-Redirect + Upgrade-Flow)</name>
  <read_first>
    - tests/e2e/minigame-gate.spec.ts (bestehendes E2E Gate-Test-Pattern)
    - tests/e2e/child-dashboard.spec.ts (Kind-Navigation-Pattern)
    - tests/fixtures/supabase.ts (adminClient, Seed-Pattern)
  </read_first>
  <files>tests/e2e/subscription-gate.spec.ts</files>
  <action>
E2E-Tests in `tests/e2e/subscription-gate.spec.ts` mit Playwright erstellen. Pattern aus `minigame-gate.spec.ts` uebernehmen.

**Fixture-Setup:** Eigene Schule + Klasse + Kind in Klasse 4 erstellen. Benenne Konstanten eindeutig:
```typescript
const SUB_E2E_SCHOOL = "Sub-E2E Testschule";
const SUB_E2E_CLASS = "Sub-E2E Klasse 4a";
const SUB_E2E_CHILD = { username: "sub.e2e", pin: "5823", grade: 4 };
```

**Tests:**

1. **Gate-Redirect**: Kind (Klasse 4, free-Schule) navigiert zu /kind/ueben -> wird zu /kind/upgrade umgeleitet -> sieht Abo-Pakete mit Preisen.

2. **Upgrade-Flow**: Kind klickt "Jetzt freischalten" bei einem Paket -> wird zu /kind/ueben umgeleitet -> kann jetzt Aufgaben loesen (ExerciseSession ist sichtbar).

3. **Kein Gate fuer Klasse 1-3**: Kind mit grade=2 (Standard-TEST_CHILD aus fixtures) navigiert zu /kind/ueben -> sieht ExerciseSession direkt, kein Redirect.

4. **Demo-Bypass**: Schule auf 'demo' setzen (via adminClient) -> Kind (Klasse 4) navigiert zu /kind/ueben -> sieht ExerciseSession direkt.

Login-Pattern aus bestehenden E2E-Tests uebernehmen (buildSyntheticEmail + padPin + page.goto('/login') + Formular ausfuellen).

Cleanup in afterAll: Testdaten loeschen.
  </action>
  <acceptance_criteria>
    - grep -q "upgrade" tests/e2e/subscription-gate.spec.ts
    - grep -q "ueben" tests/e2e/subscription-gate.spec.ts
    - grep -q "grade.*4\|grade: 4" tests/e2e/subscription-gate.spec.ts
    - grep -q "demo" tests/e2e/subscription-gate.spec.ts
    - npx playwright test tests/e2e/subscription-gate.spec.ts (alle Tests bestehen)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ozguroztopcu/Desktop/KI-Projekte/Project\ Antigravitiy/Matheapp && npx playwright test tests/e2e/subscription-gate.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Gate-Redirect-Test: Klasse-4-Kind wird von /kind/ueben zu /kind/upgrade umgeleitet
    - Upgrade-Test: Nach Klick auf Paket-Button wird Kind zu /kind/ueben geleitet und sieht Aufgaben
    - Kein-Gate-Test: Klasse-2-Kind sieht Aufgaben direkt
    - Demo-Test: Klasse-4-Kind an Demo-Schule sieht Aufgaben direkt
    - Alle Tests bestehen
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation des Subscription-Gates</name>
  <files>none</files>
  <action>
    Visuelle Pruefung des gesamten Subscription-Gate-Flows durch den Benutzer.
  </action>
  <what-built>
    Subscription-Gate fuer Klasse 4: Gate-Check in /kind/ueben, Upgrade-Seite unter /kind/upgrade mit 3 Abo-Paketen, simulierter Checkout, Demo-Bypass, und Teacher-Badge im Dashboard.
  </what-built>
  <how-to-verify>
    1. Starte Dev-Server: `npm run dev`
    2. Logge dich als Kind in Klasse 1-3 ein -> Navigiere zu /kind/ueben -> Aufgaben werden normal angezeigt (kein Gate)
    3. Logge dich als Kind in Klasse 4 ein (free-Schule) -> Navigiere zu /kind/ueben -> Du wirst zu /kind/upgrade umgeleitet
    4. Auf /kind/upgrade: Pruefe ob 3 Pakete mit Preisen (9,99 / 14,99 / 19,99 Euro) sichtbar sind
    5. Klicke "Jetzt freischalten" bei einem Paket -> Du wirst zu /kind/ueben umgeleitet, Aufgaben sind sichtbar
    6. Logge dich als Lehrer ein -> Dashboard zeigt Abo-Status-Badge (sollte jetzt den gewaehlten Paketnamen zeigen)
    7. Pruefe: Ist die Upgrade-Seite visuell ansprechend und kindgerecht (grosse Schrift, klare Buttons)?
  </how-to-verify>
  <verify>Manuelle Pruefung durch Benutzer</verify>
  <done>Benutzer hat alle 7 Schritte geprueft und "approved" gesendet</done>
  <resume-signal>Tippe "approved" oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> production DB | Tests nutzen adminClient, muessen isoliert aufgeraeumt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Information Disclosure | test fixtures | mitigate | Eindeutige Testnamen (SUB_TEST_*) + afterAll cleanup verhindert Testdaten-Leaks |
</threat_model>

<verification>
- `npx vitest run tests/integration/subscription-gate.test.ts` -- alle Tests bestehen
- `npx playwright test tests/e2e/subscription-gate.spec.ts` -- alle Tests bestehen
- `npx vitest run` -- keine Regression in bestehenden Tests
- Visuelle Verifikation der Upgrade-Seite durch Benutzer
</verification>

<success_criteria>
Phase 60 Success Criteria 1-5 aus ROADMAP.md sind durch automatisierte Tests und manuelle Verifikation abgedeckt.
</success_criteria>

<output>
After completion, create `.planning/phases/60-subscription-gate/60-02-SUMMARY.md`
</output>
