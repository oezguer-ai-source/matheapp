---
phase: 60-subscription-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql
  - lib/subscription/queries.ts
  - app/(child)/kind/ueben/page.tsx
  - app/(child)/kind/upgrade/page.tsx
  - app/(child)/kind/upgrade/actions.ts
  - app/(teacher)/lehrer/dashboard/page.tsx
autonomous: true
requirements: [REQ-05]

must_haves:
  truths:
    - "Kind in Klasse 1-3 kann ohne Einschraenkung ueben"
    - "Kind in Klasse 4 an free-Schule wird zu /kind/upgrade umgeleitet"
    - "Kind in Klasse 4 an demo-Schule kann ohne Einschraenkung ueben"
    - "Upgrade-Seite zeigt 3 Abo-Pakete mit Preisen"
    - "Jetzt-freischalten-Button setzt subscription_tier in DB und leitet zu /kind/ueben"
    - "Lehrer sehen Abo-Status ihrer Schule im Dashboard"
  artifacts:
    - path: "supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql"
      provides: "demo-Tier in CHECK-Constraint + RLS-Policy fuer Kinder"
      contains: "demo"
    - path: "lib/subscription/queries.ts"
      provides: "getSchoolSubscriptionTier Query-Funktion"
      exports: ["getSchoolSubscriptionTier"]
    - path: "app/(child)/kind/upgrade/page.tsx"
      provides: "Upgrade-Seite mit 3 Abo-Paketen"
      min_lines: 40
    - path: "app/(child)/kind/upgrade/actions.ts"
      provides: "upgradeSubscriptionAction Server Action"
      exports: ["upgradeSubscriptionAction"]
  key_links:
    - from: "app/(child)/kind/ueben/page.tsx"
      to: "lib/subscription/queries.ts"
      via: "getSchoolSubscriptionTier import"
      pattern: "getSchoolSubscriptionTier"
    - from: "app/(child)/kind/ueben/page.tsx"
      to: "/kind/upgrade"
      via: "redirect wenn Klasse 4 + free"
      pattern: "redirect.*upgrade"
    - from: "app/(child)/kind/upgrade/actions.ts"
      to: "schools.subscription_tier"
      via: "Supabase update"
      pattern: "subscription_tier"
---

<objective>
Subscription-Gate fuer Klasse-4-Inhalte implementieren: DB-Migration, RLS, Gate-Check, Upgrade-Seite und Teacher-Badge.

Purpose: Demonstriert das B2B-Freemium-Modell — Klasse 1-3 kostenlos, Klasse 4 erfordert Abo.
Output: Funktionierendes Subscription-Gate mit simuliertem Checkout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-subscription-gate/60-CONTEXT.md

<interfaces>
<!-- Bestehende DB-Struktur (aus init_schema.sql) -->
schools: id (uuid PK), name (text), subscription_tier (text, CHECK: free|grundschule|foerderung|experte), created_at
profiles: user_id (uuid PK), role (text), grade_level (int|null), display_name (text), class_id (uuid|null)
classes: id (uuid PK), school_id (uuid FK->schools), teacher_id (uuid FK->auth.users), name (text)

<!-- Bestehende RLS auf schools (nur Lehrer koennen lesen): -->
Policy "teacher_reads_own_schools": SELECT fuer authenticated WHERE id IN (SELECT school_id FROM classes WHERE teacher_id = auth.uid())
Policy "teacher_inserts_own_schools": INSERT fuer authenticated WHERE private.user_role() = 'teacher'
KEIN SELECT fuer Kinder — muss hinzugefuegt werden.

<!-- Bestehende Server-Helpers: -->
From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>
```

From lib/supabase/admin.ts:
```typescript
export function adminClient(): SupabaseClient<Database>
```

<!-- Bestehende ueben/page.tsx Struktur: -->
```typescript
// Laedt user, profile.grade_level, rendert <ExerciseSession grade={grade_level} />
// Gate-Check muss ZWISCHEN profile-Laden und ExerciseSession-Rendern eingefuegt werden
```

<!-- Lehrer-Dashboard: Zeigt profile.display_name, className, students-Tabelle -->
<!-- Badge fuer Abo-Status muss in den Header-Bereich eingefuegt werden -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration + RLS + Subscription-Query-Helper</name>
  <read_first>
    - supabase/migrations/20260415000001_init_schema.sql (CHECK-Constraint fuer schools)
    - supabase/migrations/20260415000002_rls_policies.sql (bestehende schools-Policies)
    - lib/supabase/server.ts (createClient Pattern)
    - types/database.types.ts (Database type)
  </read_first>
  <files>
    supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql,
    lib/subscription/queries.ts
  </files>
  <action>
**1. DB-Migration erstellen** (`supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql`):

Per D-12: 'demo' Tier fuer Test/Demo-Accounts hinzufuegen. Die bestehende CHECK-Constraint heisst `schools_subscription_tier_check` (automatisch generiert von Postgres aus dem inline CHECK).

```sql
-- Migration: Add 'demo' tier + child school read policy
-- Phase 60 — Subscription Gate

-- 1. Erweitere CHECK-Constraint um 'demo' (per D-12)
ALTER TABLE public.schools DROP CONSTRAINT schools_subscription_tier_check;
ALTER TABLE public.schools ADD CONSTRAINT schools_subscription_tier_check
  CHECK (subscription_tier IN ('free', 'grundschule', 'foerderung', 'experte', 'demo'));

-- 2. RLS: Kinder duerfen die Schule lesen, zu der ihre Klasse gehoert
-- Pfad: profiles.class_id -> classes.school_id -> schools.id
CREATE POLICY "child_reads_own_school"
ON public.schools FOR SELECT
TO authenticated
USING (
  (SELECT private.user_role()) = 'child'
  AND id IN (
    SELECT s.id FROM public.schools s
    JOIN public.classes c ON c.school_id = s.id
    WHERE c.id = (SELECT private.user_class_id())
  )
);

-- 3. RLS: Kinder duerfen subscription_tier ihrer Schule updaten (fuer simulierten Checkout per D-08/D-09)
-- In Produktion wuerde dies ueber einen Admin-Endpoint laufen, aber fuer die Uni-Demo
-- erlauben wir Kindern das direkte Update auf ihre eigene Schule.
CREATE POLICY "child_upgrades_own_school"
ON public.schools FOR UPDATE
TO authenticated
USING (
  (SELECT private.user_role()) = 'child'
  AND id IN (
    SELECT s.id FROM public.schools s
    JOIN public.classes c ON c.school_id = s.id
    WHERE c.id = (SELECT private.user_class_id())
  )
)
WITH CHECK (
  (SELECT private.user_role()) = 'child'
  AND id IN (
    SELECT s.id FROM public.schools s
    JOIN public.classes c ON c.school_id = s.id
    WHERE c.id = (SELECT private.user_class_id())
  )
);
```

**2. Subscription-Query-Helper** (`lib/subscription/queries.ts`):

```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "@/types/database.types";

export type SubscriptionTier = "free" | "grundschule" | "foerderung" | "experte" | "demo";

/**
 * Laedt den subscription_tier der Schule fuer das aktuelle Kind.
 * Pfad: profiles.class_id -> classes.school_id -> schools.subscription_tier
 * Gibt 'free' als Fallback zurueck wenn kein Ergebnis.
 */
export async function getSchoolSubscriptionTier(
  supabase: SupabaseClient<Database>,
  userId: string
): Promise<SubscriptionTier> {
  // 1. class_id aus Profil laden
  const { data: profile } = await supabase
    .from("profiles")
    .select("class_id")
    .eq("user_id", userId)
    .single();

  if (!profile?.class_id) return "free";

  // 2. school_id aus Klasse laden
  const { data: cls } = await supabase
    .from("classes")
    .select("school_id")
    .eq("id", profile.class_id)
    .single();

  if (!cls?.school_id) return "free";

  // 3. subscription_tier aus Schule laden
  const { data: school } = await supabase
    .from("schools")
    .select("subscription_tier")
    .eq("id", cls.school_id)
    .single();

  return (school?.subscription_tier as SubscriptionTier) ?? "free";
}

/**
 * Prueft ob ein Kind durch das Subscription-Gate blockiert wird.
 * Blockiert NUR wenn: Klasse 4 UND subscription_tier === 'free'.
 * Demo-Tier wird durchgelassen (per D-12).
 */
export function isGated(gradeLevel: number, tier: SubscriptionTier): boolean {
  return gradeLevel === 4 && tier === "free";
}
```

Die Funktion ist bewusst in zwei Teile gesplittet (Query + pure Logic) fuer einfache Testbarkeit.
  </action>
  <acceptance_criteria>
    - grep -q "demo" supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql
    - grep -q "child_reads_own_school" supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql
    - grep -q "child_upgrades_own_school" supabase/migrations/20260418000002_add_demo_tier_and_child_school_read.sql
    - grep -q "getSchoolSubscriptionTier" lib/subscription/queries.ts
    - grep -q "isGated" lib/subscription/queries.ts
    - grep -q "SubscriptionTier" lib/subscription/queries.ts
    - npx tsc --noEmit (keine Typfehler)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ozguroztopcu/Desktop/KI-Projekte/Project\ Antigravitiy/Matheapp && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Migration mit 'demo' in CHECK-Constraint existiert
    - RLS-Policy "child_reads_own_school" und "child_upgrades_own_school" in Migration
    - lib/subscription/queries.ts exportiert getSchoolSubscriptionTier, isGated, SubscriptionTier
    - Keine TypeScript-Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate-Check in ueben-Page + Upgrade-Seite + Server Action + Teacher-Badge</name>
  <read_first>
    - app/(child)/kind/ueben/page.tsx (aktuelle Struktur)
    - app/(child)/kind/dashboard/page.tsx (Kind-UI-Pattern)
    - app/(teacher)/lehrer/dashboard/page.tsx (Badge-Einfuegepunkt)
    - app/(child)/kind/spiel/actions.ts (Server Action Pattern)
    - lib/subscription/queries.ts (gerade erstellt in Task 1)
  </read_first>
  <files>
    app/(child)/kind/ueben/page.tsx,
    app/(child)/kind/upgrade/page.tsx,
    app/(child)/kind/upgrade/actions.ts,
    app/(teacher)/lehrer/dashboard/page.tsx
  </files>
  <action>
**1. Gate-Check in `/kind/ueben/page.tsx`** (per D-01, D-04):

Nach dem Profile-Laden und VOR dem ExerciseSession-Rendern den Subscription-Check einfuegen:

```typescript
import { getSchoolSubscriptionTier, isGated } from "@/lib/subscription/queries";

// ... nach profile-Laden (Zeile ~22):
const tier = await getSchoolSubscriptionTier(supabase, user.id);
if (isGated(profile.grade_level, tier)) {
  redirect("/kind/upgrade");
}
```

Per D-02: Kinder in Klasse 1-3 werden nie blockiert (isGated gibt false zurueck).
Per D-12: Demo-Tier wird durchgelassen (isGated prueft nur tier === 'free').

**2. Upgrade-Seite** (`app/(child)/kind/upgrade/page.tsx`) per D-06, D-07, D-11:

Server Component mit Kind-UI-Sprache (grosse Schrift, einfach, touch-friendly per D-11). Zeigt 3 Abo-Pakete als Cards:

```
Pakete (per D-07):
1. "Grundschulniveau" — 9,99 Euro/Monat — "Alle Aufgaben fuer Klasse 4"
2. "Foerderung" — 14,99 Euro/Monat — "Zusaetzliche Uebungen und Erklaerungen"  
3. "Experte" — 19,99 Euro/Monat — "Alle Inhalte und Premium-Features"
```

Jedes Paket hat einen "Jetzt freischalten"-Button (per D-08). Button ruft upgradeSubscriptionAction mit dem jeweiligen Tier-Wert ('grundschule', 'foerderung', 'experte') auf.

Layout: Responsive Grid (1 Spalte mobil, 3 Spalten Desktop). Farben aus der Kind-Palette (child-green, child-yellow, Slate). Grosse Buttons (h-14 minimum), grosse Schrift (text-xl bis text-3xl).

Header-Text: "Deine Schule braucht ein Abo" mit erklaeredem Untertitel.
Zurueck-Link zu /kind/dashboard am Ende.

Die Seite soll NICHT den Kind-Auth-Check duplizieren -- das Middleware erledigt bereits den Route-Schutz.

**3. Server Action** (`app/(child)/kind/upgrade/actions.ts`) per D-08, D-09, D-10:

```typescript
"use server";
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
import type { SubscriptionTier } from "@/lib/subscription/queries";

const VALID_TIERS: SubscriptionTier[] = ["grundschule", "foerderung", "experte"];

export async function upgradeSubscriptionAction(formData: FormData): Promise<void> {
  const tier = formData.get("tier") as string;
  
  // Validiere den Tier-Wert
  if (!VALID_TIERS.includes(tier as SubscriptionTier)) {
    throw new Error("Ungueltiges Abo-Paket.");
  }

  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/login");

  // Profil -> Klasse -> Schule auflösen
  const { data: profile } = await supabase
    .from("profiles")
    .select("class_id")
    .eq("user_id", user.id)
    .single();

  if (!profile?.class_id) redirect("/kind/dashboard");

  const { data: cls } = await supabase
    .from("classes")
    .select("school_id")
    .eq("id", profile.class_id)
    .single();

  if (!cls?.school_id) redirect("/kind/dashboard");

  // Subscription-Tier setzen (per D-09: kein echtes Payment)
  const { error } = await supabase
    .from("schools")
    .update({ subscription_tier: tier })
    .eq("id", cls.school_id);

  if (error) throw new Error("Upgrade fehlgeschlagen.");

  // Per D-10: Redirect zu /kind/ueben nach Upgrade
  redirect("/kind/ueben");
}
```

**4. Teacher-Badge** (per D-14):

Im Lehrer-Dashboard (`app/(teacher)/lehrer/dashboard/page.tsx`) einen kleinen Badge einfuegen der den Abo-Status der Schule zeigt. Dafuer:

a) Nach dem Klassen-Laden (Zeile ~42-49) auch school_id und subscription_tier laden:
```typescript
let schoolTier: string | null = null;
if (profile.class_id) {
  const { data: classData } = await supabase
    .from("classes")
    .select("name, school_id")
    .eq("id", profile.class_id)
    .maybeSingle();
  className = classData?.name ?? null;
  
  if (classData?.school_id) {
    const { data: schoolData } = await supabase
      .from("schools")
      .select("subscription_tier")
      .eq("id", classData.school_id)
      .maybeSingle();
    schoolTier = schoolData?.subscription_tier ?? null;
  }
}
```

b) Badge im Header-Bereich unter dem Klassennamen einfuegen:
```tsx
{schoolTier && (
  <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${
    schoolTier === "free" 
      ? "bg-slate-100 text-slate-600" 
      : "bg-green-100 text-green-700"
  }`}>
    Abo: {schoolTier === "free" ? "Kostenlos" : schoolTier.charAt(0).toUpperCase() + schoolTier.slice(1)}
  </span>
)}
```

Der Badge zeigt "Kostenlos" fuer free-Tier und den Paketnamen fuer paid-Tiers. Gruen wenn bezahlt, grau wenn free.
  </action>
  <acceptance_criteria>
    - grep -q "getSchoolSubscriptionTier" "app/(child)/kind/ueben/page.tsx"
    - grep -q "isGated" "app/(child)/kind/ueben/page.tsx"
    - grep -q "redirect.*upgrade" "app/(child)/kind/ueben/page.tsx"
    - test -f "app/(child)/kind/upgrade/page.tsx"
    - grep -q "9,99" "app/(child)/kind/upgrade/page.tsx"
    - grep -q "14,99" "app/(child)/kind/upgrade/page.tsx"
    - grep -q "19,99" "app/(child)/kind/upgrade/page.tsx"
    - grep -q "upgradeSubscriptionAction" "app/(child)/kind/upgrade/actions.ts"
    - grep -q "subscription_tier" "app/(child)/kind/upgrade/actions.ts"
    - grep -q "redirect.*ueben" "app/(child)/kind/upgrade/actions.ts"
    - grep -q "subscription_tier\|schoolTier\|Abo" "app/(teacher)/lehrer/dashboard/page.tsx"
    - npx tsc --noEmit (keine Typfehler)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ozguroztopcu/Desktop/KI-Projekte/Project\ Antigravitiy/Matheapp && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ueben/page.tsx leitet Klasse-4-Kinder an free-Schulen zu /kind/upgrade um
    - ueben/page.tsx laesst Klasse 1-3 und demo-Tier durch
    - Upgrade-Seite zeigt 3 Pakete mit Preisen 9,99 / 14,99 / 19,99
    - "Jetzt freischalten" setzt subscription_tier in DB und leitet zu /kind/ueben
    - Teacher-Dashboard zeigt Abo-Status-Badge
    - Keine TypeScript-Fehler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child -> upgrade action | Kind kann beliebigen tier-Wert senden |
| child -> schools UPDATE | Kind koennte fremde Schulen upgraden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | upgradeSubscriptionAction | mitigate | Validiere tier-Wert gegen Allowlist VALID_TIERS; nur 'grundschule', 'foerderung', 'experte' erlaubt |
| T-60-02 | Elevation | child_upgrades_own_school RLS | mitigate | RLS-Policy beschraenkt UPDATE auf die eigene Schule (ueber class_id -> school_id Pfad) |
| T-60-03 | Spoofing | upgrade action auth | mitigate | supabase.auth.getUser() vor jeder DB-Operation; redirect bei fehlendem User |
| T-60-04 | Information Disclosure | subscription_tier | accept | Tier-Info ist nicht sensibel; Kind sieht nur eigene Schule (RLS) |
</threat_model>

<verification>
- Kind in Klasse 1-3 erreicht ExerciseSession ohne Redirect
- Kind in Klasse 4 an free-Schule wird zu /kind/upgrade umgeleitet
- Kind in Klasse 4 an demo-Schule erreicht ExerciseSession ohne Redirect
- Upgrade-Button setzt subscription_tier und leitet zu /kind/ueben
- Lehrer-Dashboard zeigt Abo-Badge
- TypeScript kompiliert fehlerfrei
</verification>

<success_criteria>
Das Subscription-Gate blockiert Klasse-4-Inhalte bei free-Schulen, erlaubt Demo- und bezahlte Tiers, und der simulierte Checkout funktioniert end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/60-subscription-gate/60-01-SUMMARY.md`
</output>
