---
phase: 10-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/favicon.ico
  - eslint.config.mjs
  - components.json
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/label.tsx
  - components/ui/card.tsx
  - components/ui/alert.tsx
  - components/ui/tabs.tsx
  - lib/utils.ts
  - README.md
  - .npmrc
  - next-env.d.ts
autonomous: true
requirements:
  - REQ-01
must_haves:
  truths:
    - "The Next.js 15.2 development server starts with `npm run dev` on port 3000"
    - "The root layout declares `lang=\"de\"` per D-21"
    - "TypeScript strict mode is enabled"
    - "Tailwind CSS v4 compiles (test utility like `text-4xl` works)"
    - "shadcn/ui components can be imported from `@/components/ui/*`"
    - "shadcn/ui is initialized but no child-facing UI uses it (D-17/D-18 boundary established in folder layout)"
  artifacts:
    - path: package.json
      provides: "Next 15.2, React 19, Tailwind v4, TypeScript 5"
      contains: "\"next\": \"15.2"
    - path: app/layout.tsx
      provides: "Root layout with lang=de"
      contains: "lang=\"de\""
    - path: components.json
      provides: "shadcn/ui registry config"
      contains: "\"style\""
    - path: lib/utils.ts
      provides: "`cn()` helper used by shadcn/ui components"
      exports: ["cn"]
    - path: components/ui/button.tsx
      provides: "shadcn Button component (Teacher-scope only per D-18)"
    - path: components/ui/input.tsx
      provides: "shadcn Input component"
    - path: components/ui/label.tsx
      provides: "shadcn Label component"
    - path: components/ui/card.tsx
      provides: "shadcn Card component"
    - path: components/ui/alert.tsx
      provides: "shadcn Alert component"
    - path: components/ui/tabs.tsx
      provides: "shadcn Tabs component (role toggle uses native tabs; this is a fallback)"
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: "Tailwind v4 @import"
      pattern: "import.*globals\\.css"
    - from: components/ui/button.tsx
      to: lib/utils.ts
      via: "import { cn } from"
      pattern: "from [\"']@/lib/utils"
---

<objective>
Scaffold the Next.js 15.2 application with TypeScript + Tailwind v4 via `create-next-app`, then initialize shadcn/ui and add the six components that the teacher-side UI will consume in Plan 08 (Button, Input, Label, Card, Alert, Tabs). Establish the route-group folder boundary (app/(child) vs app/(teacher)) so D-17/D-18 is structurally enforced from day one.

Purpose: Every subsequent plan assumes a runnable Next.js app with the exact stack locked in STACK.md (Next 15.2, React 19, Tailwind v4, TypeScript 5). shadcn/ui components are needed in Wave 5 for the teacher login form (Plan 08) — pre-installing them keeps Plan 08 focused on UI assembly.
Output: A functional `matheapp` Next.js project in the repo root with `npm run dev` running on :3000, shadcn/ui initialized with Slate base + New York style, six components committed in `components/ui/`, and empty route-group folders ready for Plans 08–09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-UI-SPEC.md
@.planning/research/STACK.md
@.planning/phases/10-foundation/10-01-SUMMARY.md

<interfaces>
<!-- Stack contract locked by STACK.md and RESEARCH.md Standard Stack table -->

Versions to pin exactly (per RESEARCH.md § Version verification 2026-04-15):
- next: 15.2.x  (NOT 16 — Next.js 16 renames middleware.ts to proxy.ts)
- react: 19.x   (bundled with Next 15)
- typescript: 5.x
- tailwindcss: 4.x
- @supabase/supabase-js, @supabase/ssr, zod — installed in Plan 04/07, NOT here

shadcn/ui config (per 10-UI-SPEC.md § Design System):
- Style: New York
- Base color: Slate
- CSS Variables: yes
- Alias: `@/components`, `@/lib/utils`
- Components needed in Phase 10: button, input, label, card, alert, tabs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15.2 with create-next-app</name>
  <read_first>
    - .planning/research/STACK.md (§ Core Framework, § Full Installation Commands)
    - .planning/phases/10-foundation/10-RESEARCH.md (§ Standard Stack, § Installation, § Recommended Project Structure)
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, app/favicon.ico, eslint.config.mjs, README.md, .npmrc, next-env.d.ts</files>
  <action>
    1. From the repository root, run create-next-app non-interactively and pin Next.js 15.2 (NOT 16):

    ```bash
    npx create-next-app@15.2 . \
      --typescript \
      --tailwind \
      --app \
      --eslint \
      --no-src-dir \
      --no-turbopack \
      --import-alias "@/*" \
      --use-npm \
      --yes
    ```

    The `.` installs into the current directory (repo root); `--yes` skips all prompts. `--no-src-dir` places `app/` directly under the repo root (matches RESEARCH.md § Recommended Project Structure). `--no-turbopack` is chosen because the Supabase-SSR cookie helper historically has more regressions on Turbopack; switch to Turbopack only after Plan 10 green-lights the full test suite.

    2. Verify the pinned versions by running `npm view` against the local `package.json`:
       - `"next": "15.2.x"` (any 15.2 patch is fine)
       - `"react": "^19"` or `"19.x"`
       - `"typescript": "^5"`
       - `"tailwindcss": "^4"`

    3. Open the generated `app/layout.tsx` and change the `<html lang="en">` attribute to `<html lang="de">` per CONTEXT.md D-21 (German UI). Also set:
       - `<title>` metadata: default `"Matheapp"`
       - `<meta name="description">`: `"Mathe lernen in der Grundschule"`

    4. Open the generated `app/page.tsx` and REPLACE its contents with a single redirect to `/login`:

    ```tsx
    import { redirect } from "next/navigation";

    export default function Home() {
      redirect("/login");
    }
    ```

    5. Verify the dev server starts cleanly:

    ```bash
    npm run dev &
    SERVER_PID=$!
    sleep 6
    curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/
    kill $SERVER_PID
    ```

    Expected HTTP 307 (redirect from `/` to `/login` — which doesn't exist yet and will 404, that's fine for this plan; the 307 proves `app/page.tsx` ran).

    6. Commit `package-lock.json` (not just `package.json`) so downstream plans get deterministic installs.
  </action>
  <verify>
    <automated>test -f package.json && test -f next.config.ts && test -f app/layout.tsx && node -e "const p=require('./package.json'); const ok = p.dependencies.next && p.dependencies.next.startsWith('15.2') && p.dependencies.react && p.dependencies.react.match(/^\\^?19/); process.exit(ok ? 0 : 1)" && grep -q 'lang="de"' app/layout.tsx && grep -q 'redirect("/login")' app/page.tsx && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.next` starts with `15.2` (NOT `16`, NOT `^15.0`)
    - `package.json` `dependencies.react` matches `/^\^?19/`
    - `package.json` `devDependencies.typescript` exists (any `^5`)
    - `package.json` `devDependencies.tailwindcss` matches `/^\^?4/`
    - `app/layout.tsx` contains the literal string `lang="de"`
    - `app/page.tsx` imports `redirect` from `next/navigation` and calls `redirect("/login")`
    - `tsconfig.json` has `"strict": true`
    - `npm run dev` starts on port 3000 without errors and `GET /` returns HTTP 307 (redirect)
  </acceptance_criteria>
  <done>`npm install && npm run dev` works, `GET /` redirects to `/login`, HTML root element has `lang="de"`.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add the six Phase-10 components</name>
  <read_first>
    - .planning/phases/10-foundation/10-UI-SPEC.md (§ Design System, § Component Inventory)
    - .planning/phases/10-foundation/10-CONTEXT.md (D-17, D-18, D-20)
  </read_first>
  <files>components.json, components/ui/button.tsx, components/ui/input.tsx, components/ui/label.tsx, components/ui/card.tsx, components/ui/alert.tsx, components/ui/tabs.tsx, lib/utils.ts, app/globals.css</files>
  <action>
    1. Initialize shadcn/ui non-interactively with the preset declared in 10-UI-SPEC.md:

    ```bash
    npx shadcn@latest init --yes --base-color slate --style new-york
    ```

    This writes `components.json`, creates `lib/utils.ts` with the `cn()` helper, and updates `app/globals.css` with shadcn CSS variables.

    2. Add the six components needed in Phase 10:

    ```bash
    npx shadcn@latest add button input label card alert tabs --yes
    ```

    Each command writes to `components/ui/<component>.tsx`. Verify each file exists before moving on.

    3. Create the route-group folders so Plan 08 + Plan 09 can slot into them without ambiguity. Use `.gitkeep` files since the route-group folders will be populated by later plans:

    ```bash
    mkdir -p "app/(child)/kind/dashboard"
    mkdir -p "app/(teacher)/lehrer/dashboard"
    mkdir -p "app/login"
    mkdir -p "app/registrieren"
    mkdir -p "components/login"
    mkdir -p "components/child"
    mkdir -p "lib/supabase"
    mkdir -p "lib/schemas"
    mkdir -p "types"
    touch "app/(child)/.gitkeep" "app/(teacher)/.gitkeep" "components/login/.gitkeep" "components/child/.gitkeep" "lib/supabase/.gitkeep" "lib/schemas/.gitkeep" "types/.gitkeep"
    ```

    This structurally enforces the D-17/D-18 boundary: child-facing code lives under `app/(child)/` and `components/child/` with Tailwind only; teacher-facing code lives under `app/(teacher)/` and uses shadcn components from `components/ui/`.

    4. Verify shadcn integration by running `tsc --noEmit`:

    ```bash
    npx tsc --noEmit
    ```

    Expected: no TypeScript errors.

    Per D-18: shadcn/ui is initialized but child-facing UI must NEVER import from `components/ui/*`. This is a convention enforced by code review and the folder layout — there is no ESLint rule for it in Phase 10 (could be added later).
  </action>
  <verify>
    <automated>test -f components.json && test -f lib/utils.ts && test -f components/ui/button.tsx && test -f components/ui/input.tsx && test -f components/ui/label.tsx && test -f components/ui/card.tsx && test -f components/ui/alert.tsx && test -f components/ui/tabs.tsx && test -d "app/(child)/kind/dashboard" && test -d "app/(teacher)/lehrer/dashboard" && test -d "app/login" && test -d "app/registrieren" && grep -q "export.*cn" lib/utils.ts && npx tsc --noEmit && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists with `"style": "new-york"` and `"baseColor": "slate"`
    - `lib/utils.ts` exports a `cn` function (shadcn's class-merge helper)
    - All six component files exist under `components/ui/`: button.tsx, input.tsx, label.tsx, card.tsx, alert.tsx, tabs.tsx
    - Each component file imports `cn` from `@/lib/utils`
    - Route-group directories exist: `app/(child)/kind/dashboard`, `app/(teacher)/lehrer/dashboard`, `app/login`, `app/registrieren`
    - Helper directories exist: `components/login`, `components/child`, `lib/supabase`, `lib/schemas`, `types`
    - `npx tsc --noEmit` passes (no type errors from the new shadcn files)
  </acceptance_criteria>
  <done>shadcn/ui is initialized with Slate + New York preset, six components are available under `components/ui/`, and the route-group folder boundary between child and teacher code is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Untrusted third-party code enters here |
| Child route group -> Teacher route group | UI-primitive leakage boundary (D-17/D-18) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | Transitive npm packages during `create-next-app` / `shadcn add` | mitigate | Pin `next@15.2` explicitly; commit `package-lock.json` for deterministic installs; downstream plans `npm ci` instead of `npm install` |
| T-10-02-02 | Elevation of Privilege | Child component importing from `@/components/ui/*` (violates D-18) | accept | Enforced by folder convention + code review in Phase 10; a lint rule can be added in a later phase if drift occurs. Impact: stylistic, not security. |
| T-10-02-03 | Information Disclosure | Next.js `reactStrictMode` or `poweredByHeader` exposing version | accept | Low value; Next.js default. Can disable `poweredByHeader` in later hardening phase. |
</threat_model>

<verification>
- `npm install && npm run dev` runs on port 3000 with no errors
- `GET /` redirects (307) to `/login`
- `<html lang="de">` is present in `app/layout.tsx`
- `npx tsc --noEmit` passes
- All six shadcn components are committed
- Route-group directories are in place to receive Plan 08 and Plan 09 output
</verification>

<success_criteria>
Next.js 15.2 project scaffolded with Tailwind v4, TypeScript strict, React 19. shadcn/ui initialized with Slate + New York. Six components (Button, Input, Label, Card, Alert, Tabs) available under `components/ui/`. Folder structure matches RESEARCH.md § Recommended Project Structure and enforces the D-17/D-18 UI boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-02-SUMMARY.md`
</output>
