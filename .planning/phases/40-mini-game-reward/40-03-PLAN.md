---
phase: 40-mini-game-reward
plan: 03
type: execute
wave: 2
depends_on: [40-01, 40-02]
files_modified:
  - tests/unit/balloon-game.test.tsx
  - tests/integration/start-game-action.test.ts
  - tests/e2e/minigame-gate.spec.ts
autonomous: false
requirements: [REQ-03]
must_haves:
  truths:
    - "BalloonGame rendert Ballon-Elemente und Score-Counter"
    - "BalloonGame hat keinen direkten Supabase-Import (SC-5)"
    - "startGameAction zieht Punkte ab und schreibt negative progress_entry"
    - "startGameAction lehnt ab wenn Punkte < 500"
    - "Dashboard zeigt 'Spiel starten' Link wenn Punkte ausreichen"
    - "Visuelles Verhalten stimmt mit Design ueberein"
  artifacts:
    - path: "tests/unit/balloon-game.test.tsx"
      provides: "Unit-Tests fuer BalloonGame Komponente"
      contains: "BalloonGame"
    - path: "tests/integration/start-game-action.test.ts"
      provides: "Integration-Tests fuer startGameAction Server Action"
      contains: "startGameAction"
    - path: "tests/e2e/minigame-gate.spec.ts"
      provides: "E2E-Tests fuer Punkt-Gate und Dashboard-Button"
      contains: "minigame"
  key_links:
    - from: "tests/unit/balloon-game.test.tsx"
      to: "components/child/balloon-game.tsx"
      via: "import und render test"
      pattern: "BalloonGame"
    - from: "tests/integration/start-game-action.test.ts"
      to: "app/(child)/kind/spiel/actions.ts"
      via: "Supabase-basierter Integrationstest"
      pattern: "startGameAction"
    - from: "tests/e2e/minigame-gate.spec.ts"
      to: "/kind/dashboard"
      via: "Playwright Browser-Test"
      pattern: "Spiel starten"
---

<objective>
Automatisierte Tests fuer das Mini-Game: Unit-Tests fuer die BalloonGame-Komponente, Integration-Tests fuer die startGameAction Server Action, und E2E-Tests fuer das Punkt-Gating. Plus visuelle Verifikation durch den Benutzer.

Purpose: Verifiziere dass das Belohnungssystem korrekt funktioniert — Punkt-Abzug, Gating, und die Spiel-Komponente verhalten sich wie spezifiziert. Stelle sicher dass das Spiel KEINE Punkte zurueckschreibt (SC-5).
Output: 3 Test-Dateien mit vollstaendiger Abdeckung der Phase-40 Success Criteria
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-mini-game-reward/40-RESEARCH.md
@.planning/phases/40-mini-game-reward/40-01-SUMMARY.md
@.planning/phases/40-mini-game-reward/40-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From components/child/balloon-game.tsx:
```typescript
export function BalloonGame({ currentPoints }: { currentPoints: number }): JSX.Element;
// Imports: startGameAction from actions, Balloon, GameOverScreen
// States: idle, starting, playing, over
// GAME_DURATION = 75, MAX_BALLOONS = 12, SPAWN_INTERVAL = 800
```

From app/(child)/kind/spiel/actions.ts:
```typescript
export async function startGameAction(): Promise<{ success: boolean; error?: string }>;
// Checks auth, verifies points >= 500, inserts negative progress_entry
```

From tests/fixtures/supabase.ts (existing pattern):
```typescript
// adminClient for seeding test data
// buildSyntheticEmail, padPin for child account creation
// seedTestData, cleanupTestData pattern
```

From tests/e2e/child-dashboard.spec.ts (existing E2E pattern):
```typescript
// Playwright test structure: seed data -> login -> navigate -> assert
// Uses adminClient to create progress_entries for deterministic testing
```

Existing vitest.config.ts:
```typescript
// include: ['tests/unit/**/*.test.{ts,tsx}', 'tests/integration/**/*.test.ts']
```

Existing playwright.config.ts:
```typescript
// testDir: 'tests/e2e'
// baseURL: 'http://localhost:3000'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit-Tests BalloonGame + Integration-Tests startGameAction + E2E-Tests Minigame-Gate</name>
  <read_first>
    - components/child/balloon-game.tsx
    - components/child/balloon.tsx
    - components/child/game-over-screen.tsx
    - app/(child)/kind/spiel/actions.ts
    - app/(child)/kind/spiel/page.tsx
    - app/(child)/kind/dashboard/page.tsx
    - tests/fixtures/supabase.ts
    - tests/unit/number-pad.test.tsx
    - tests/unit/feedback-overlay.test.tsx
    - tests/integration/submit-answer-action.test.ts
    - tests/e2e/child-dashboard.spec.ts
    - vitest.config.ts
    - playwright.config.ts
  </read_first>
  <files>
    tests/unit/balloon-game.test.tsx,
    tests/integration/start-game-action.test.ts,
    tests/e2e/minigame-gate.spec.ts
  </files>
  <action>
1. Erstelle `tests/unit/balloon-game.test.tsx` — Unit-Tests fuer BalloonGame (REQ-03-SC3, REQ-03-SC5):
   - Import: render, screen, fireEvent aus @testing-library/react
   - Import: vi, describe, it, expect aus vitest
   - Mock startGameAction: `vi.mock("@/app/(child)/kind/spiel/actions", () => ({ startGameAction: vi.fn() }))` — return `{ success: true }`
   
   Tests:
   a) "rendert Spielstart-Screen im idle State" — BalloonGame rendert "Ballonplatzen!" Heading und "Spiel starten" Button
   b) "zeigt Score-Counter bei gameState playing" — nach Klick auf "Spiel starten" (mit gemockter Action) erscheint Score-Anzeige
   c) "BalloonGame importiert NICHT direkt von Supabase" (REQ-03-SC5):
      - Lese den Quellcode der Datei components/child/balloon-game.tsx
      - Pruefe dass kein `from "@/lib/supabase` oder `from "@supabase` Import vorhanden ist
      - ALTERNATIVE: Nutze statische Analyse statt Runtime-Test:
        ```typescript
        import { readFileSync } from "fs";
        import { resolve } from "path";
        
        it("importiert nicht direkt von Supabase (SC-5)", () => {
          const source = readFileSync(
            resolve(__dirname, "../../components/child/balloon-game.tsx"),
            "utf-8"
          );
          expect(source).not.toMatch(/from\s+["']@\/lib\/supabase/);
          expect(source).not.toMatch(/from\s+["']@supabase/);
        });
        ```
   d) "zeigt Fehlermeldung wenn startGameAction fehlschlaegt" — mocke Action mit `{ success: false, error: "Nicht genug Punkte." }`, pruefe dass Fehler angezeigt wird
   e) "Balloon-Komponente rendert mit korrekten Props" — Rendere Balloon direkt, pruefe aria-label und onClick-Handler
   f) "GameOverScreen zeigt Score und Dashboard-Link" — Rendere GameOverScreen mit score=15, pruefe "15" und "/kind/dashboard" Link

2. Erstelle `tests/integration/start-game-action.test.ts` — Integration-Tests (REQ-03-SC4):
   Folge dem bestehenden Pattern aus `tests/integration/submit-answer-action.test.ts`:
   
   - Import adminClient und Test-Fixtures
   - Setup: Erstelle Test-Kind mit 600 Punkten (via adminClient INSERT in progress_entries, 6x100 Punkte fuer verschiedene operation_types)
   - Teardown: Loesche Test-Daten
   
   HINWEIS: Da Server Actions Next.js-internen Server-Kontext benoetigen (cookies, headers), koennen sie nicht direkt in Vitest aufgerufen werden. Stattdessen teste die LOGIK durch:
   
   a) "progress_entries akzeptiert operation_type 'minigame_redeem'" — Direkt via adminClient INSERT mit operation_type = 'minigame_redeem' und points_earned = -500, verifiziere dass der INSERT erfolgreich ist (kein Error)
   b) "negative points_earned Werte werden akzeptiert" — Verifiziere INSERT mit points_earned = -500, pruefe dass der Wert korrekt gespeichert ist
   c) "Punkt-Summe berechnet korrekt mit negativem Eintrag" — Seede 600 Punkte + 1x minigame_redeem (-500), berechne SUM via select+reduce, erwarte 100
   d) "Zweite Einloesung bei nur 100 Punkten nicht moeglich" — Verifiziere dass die Punkt-Summe nach Einloesung unter MINIGAME_THRESHOLD liegt (100 < 500)

3. Erstelle `tests/e2e/minigame-gate.spec.ts` — E2E-Tests (REQ-03-SC2):
   Folge dem bestehenden Pattern aus `tests/e2e/child-dashboard.spec.ts`:
   
   - Import test, expect aus @playwright/test
   - Import adminClient und Fixtures
   
   Setup (beforeAll): 
   - Erstelle Test-Kind via adminClient (buildSyntheticEmail + padPin Pattern)
   - Seede progress_entries mit 600 Punkten
   
   Teardown (afterAll):
   - Loesche Test-Kind und zugehoerige Daten
   
   Tests:
   a) "Dashboard zeigt aktiven 'Spiel starten'-Link bei >= 500 Punkten":
      - Login als Test-Kind
      - Navigiere zu /kind/dashboard
      - Erwarte: Link mit Text "Spiel starten" und href="/kind/spiel" ist sichtbar und klickbar
   
   b) "Dashboard zeigt deaktivierten 'Spiel starten' bei < 500 Punkten":
      - Erstelle zweites Test-Kind mit nur 100 Punkten
      - Login als dieses Kind
      - Navigiere zu /kind/dashboard
      - Erwarte: Element mit Text "Spiel starten" ist sichtbar aber hat Klasse "cursor-not-allowed"
      - Erwarte: KEIN Link zu "/kind/spiel" vorhanden
   
   c) "/kind/spiel redirectet zu /kind/dashboard bei < 500 Punkten":
      - Login als Kind mit < 500 Punkten
      - Navigiere direkt zu /kind/spiel
      - Erwarte: Redirect zu /kind/dashboard (URL aendert sich)
   
   d) "/kind/spiel ist erreichbar bei >= 500 Punkten":
      - Login als Kind mit >= 500 Punkten  
      - Navigiere zu /kind/spiel
      - Erwarte: Seite laedt (kein Redirect), "Ballonplatzen" oder "Spiel starten" Button sichtbar
  </action>
  <acceptance_criteria>
    - Unit-Test-Datei existiert: `test -f tests/unit/balloon-game.test.tsx`
    - Unit-Tests pruefen SC-5 (kein Supabase-Import): `grep -q "supabase" tests/unit/balloon-game.test.tsx`
    - Integration-Test-Datei existiert: `test -f tests/integration/start-game-action.test.ts`
    - Integration-Tests pruefen minigame_redeem: `grep -q "minigame_redeem" tests/integration/start-game-action.test.ts`
    - Integration-Tests pruefen negative Punkte: `grep -q "\-500" tests/integration/start-game-action.test.ts`
    - E2E-Test-Datei existiert: `test -f tests/e2e/minigame-gate.spec.ts`
    - E2E-Tests pruefen Dashboard-Link: `grep -q "Spiel starten" tests/e2e/minigame-gate.spec.ts`
    - E2E-Tests pruefen Redirect: `grep -q "/kind/dashboard" tests/e2e/minigame-gate.spec.ts`
    - Unit-Tests laufen: `npx vitest run tests/unit/balloon-game.test.tsx`
    - Integration-Tests laufen: `npx vitest run tests/integration/start-game-action.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/balloon-game.test.tsx && npx vitest run tests/integration/start-game-action.test.ts && echo "PASS"</automated>
  </verify>
  <done>
    - Unit-Tests: BalloonGame Rendering, Score, kein Supabase-Import (SC-5), Fehlerbehandlung, Balloon Props, GameOverScreen
    - Integration-Tests: minigame_redeem operation_type wird akzeptiert, negative Punkte funktionieren, Punkt-Summe korrekt
    - E2E-Tests: Dashboard-Button aktiv/inaktiv, /kind/spiel Redirect bei < 500, Zugang bei >= 500
    - Alle Unit- und Integration-Tests gruen
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle Verifikation des Mini-Game Belohnungssystems</name>
  <files>app/(child)/kind/spiel/page.tsx, app/(child)/kind/dashboard/page.tsx</files>
  <action>
Starte den Dev-Server (`npm run dev`) und fuehre die folgenden manuellen Pruefungen durch:

1. Login als Kind mit >= 500 Punkten
2. Dashboard pruefen:
   - "Spiel starten"-Button ist gelb und klickbar
   - ProgressBar zeigt "Spiel freigeschaltet!"
3. Klicke "Spiel starten" auf Dashboard -> /kind/spiel oeffnet sich
4. Klicke "Spiel starten" auf der Spiel-Seite
5. Spiel pruefen:
   - Ballons steigen von unten nach oben
   - Klick/Touch auf Ballon = Platz-Animation + Score erhoeht sich
   - Timer zaehlt von 75 herunter
   - Score-Counter oben sichtbar
6. Warte bis Timer ablaeuft -> Game Over Screen
   - Zeigt Ballon-Score
   - "Zurueck zum Dashboard"-Button fuehrt zu /kind/dashboard
7. Dashboard nach Spiel:
   - Punkte-Anzeige zeigt reduzierte Punkte (500 weniger)
   - Wenn < 500 Punkte: "Spiel starten" ist jetzt grau/deaktiviert
8. Test: Gib /kind/spiel direkt in die URL-Leiste ein mit einem Kind das < 500 Punkte hat -> sollte zu /kind/dashboard redirecten
  </action>
  <verify>
    <automated>echo "Manual verification required — run npm run dev and test visually"</automated>
  </verify>
  <done>
    - Spiel ist visuell ansprechend und funktional fuer Kinder 6-10
    - Ballons steigen, platzen beim Klick, Score zaehlt korrekt
    - Timer laeuft ab und zeigt Game-Over Screen
    - Dashboard zeigt korrekte Punkte nach Spiel
    - Punkt-Gating funktioniert (Redirect bei < 500 Punkten)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Produktion | Tests muessen reale DB-Interaktion verifizieren (operation_type, negative Punkte) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Information Disclosure | Test Fixtures | mitigate | Test-Daten werden in afterAll geloescht; Test-Accounts nutzen eindeutige Namen |
| T-40-08 | Tampering | SC-5 Verification | mitigate | Statische Analyse in Unit-Test verifiziert dass BalloonGame keinen Supabase-Import hat |
</threat_model>

<verification>
- `npx vitest run tests/unit/balloon-game.test.tsx` — alle Unit-Tests gruen
- `npx vitest run tests/integration/start-game-action.test.ts` — alle Integration-Tests gruen
- `npx playwright test tests/e2e/minigame-gate.spec.ts` — alle E2E-Tests gruen (benoetigt laufenden Dev-Server)
- Human-Verify: Visuelles Spiel funktioniert, Punkte werden abgezogen, Gating greift
</verification>

<success_criteria>
- Alle Unit-Tests bestehen (BalloonGame, Balloon, GameOverScreen, SC-5 kein Supabase-Import)
- Alle Integration-Tests bestehen (minigame_redeem INSERT, negative Punkte, Summen-Berechnung)
- Alle E2E-Tests bestehen (Dashboard-Button, Redirect-Gate, Spiel-Zugang)
- Menschliche Verifikation: Spiel ist spielbar, Punkte werden korrekt abgezogen
</success_criteria>

<output>
After completion, create `.planning/phases/40-mini-game-reward/40-03-SUMMARY.md`
</output>
