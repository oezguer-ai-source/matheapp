---
phase: 40-mini-game-reward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260418000001_add_minigame_redeem_operation.sql
  - app/(child)/kind/spiel/actions.ts
  - app/(child)/kind/spiel/page.tsx
  - app/(child)/kind/dashboard/page.tsx
autonomous: false
requirements: [REQ-03]
must_haves:
  truths:
    - "Server Action startGameAction zieht 500 Punkte als negative progress_entry ab"
    - "Spiel-Seite /kind/spiel redirectet zu /kind/dashboard wenn Punkte < 500"
    - "Dashboard zeigt aktiven 'Spiel starten'-Link wenn Punkte >= 500"
    - "Dashboard zeigt deaktivierten 'Spiel starten'-Button wenn Punkte < 500"
    - "operation_type 'minigame_redeem' ist in der DB erlaubt"
  artifacts:
    - path: "supabase/migrations/20260418000001_add_minigame_redeem_operation.sql"
      provides: "CHECK Constraint Erweiterung fuer minigame_redeem"
      contains: "minigame_redeem"
    - path: "app/(child)/kind/spiel/actions.ts"
      provides: "Server Action fuer Punkt-Abzug"
      exports: ["startGameAction"]
    - path: "app/(child)/kind/spiel/page.tsx"
      provides: "Server-Side Punkt-Gating"
      contains: "redirect"
    - path: "app/(child)/kind/dashboard/page.tsx"
      provides: "Dashboard mit Spiel-Button"
      contains: "Spiel starten"
  key_links:
    - from: "app/(child)/kind/spiel/page.tsx"
      to: "progress_entries"
      via: "Supabase SUM query"
      pattern: "points_earned"
    - from: "app/(child)/kind/spiel/actions.ts"
      to: "progress_entries"
      via: "INSERT negative entry"
      pattern: "minigame_redeem"
    - from: "app/(child)/kind/dashboard/page.tsx"
      to: "/kind/spiel"
      via: "Link wenn Punkte >= Threshold"
      pattern: "MINIGAME_THRESHOLD"
---

<objective>
Server-seitiges Punkt-Gating und Punkt-Abzug fuer das Mini-Game, plus DB-Migration fuer den neuen operation_type und Dashboard-Integration.

Purpose: Ermoeglicht den Belohnungs-Loop — Kinder muessen Punkte verdienen, koennen sie fuer das Spiel einloesen, und muessen dann erneut ueben. Server-Side Checks verhindern Client-Manipulation (D-12, D-13, D-16).
Output: DB-Migration, Server Action, Spiel-Seite mit Gating, Dashboard-Button
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-mini-game-reward/40-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/config/rewards.ts:
```typescript
export const MINIGAME_THRESHOLD = 500;
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>;
```

From app/(child)/kind/ueben/actions.ts (Server Action pattern):
```typescript
// Pattern: "use server" + createClient() + getUser() + safeParse + INSERT + return {success, error}
```

From app/(child)/kind/dashboard/page.tsx:
```typescript
// Pattern: createClient() -> getUser() -> profile query -> progress_entries query -> reduce for totalPoints
// Currently renders: DashboardStats + ProgressBar + Link to /kind/ueben + LogoutButtonChild
```

From supabase/migrations/20260415000001_init_schema.sql:
```sql
create table public.progress_entries (
  id uuid primary key default gen_random_uuid(),
  child_id uuid not null references auth.users(id) on delete cascade,
  operation_type text not null
    check (operation_type in ('addition', 'subtraktion', 'multiplikation', 'division')),
  grade int not null check (grade between 1 and 4),
  correct boolean not null,
  points_earned int not null default 0,
  created_at timestamptz not null default now()
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration + Server Action startGameAction + Spiel-Seite mit Gating</name>
  <read_first>
    - supabase/migrations/20260415000001_init_schema.sql
    - app/(child)/kind/ueben/actions.ts
    - app/(child)/kind/dashboard/page.tsx
    - lib/config/rewards.ts
    - lib/supabase/server.ts
  </read_first>
  <files>
    supabase/migrations/20260418000001_add_minigame_redeem_operation.sql,
    app/(child)/kind/spiel/actions.ts,
    app/(child)/kind/spiel/page.tsx
  </files>
  <action>
1. Erstelle DB-Migration `supabase/migrations/20260418000001_add_minigame_redeem_operation.sql`:
   - DROP den bestehenden CHECK Constraint auf progress_entries.operation_type
   - ADD neuen CHECK Constraint der 'minigame_redeem' zusaetzlich erlaubt:
   ```sql
   -- Phase 40: Extend operation_type CHECK to allow minigame point redemption (D-13)
   ALTER TABLE public.progress_entries
     DROP CONSTRAINT progress_entries_operation_type_check;

   ALTER TABLE public.progress_entries
     ADD CONSTRAINT progress_entries_operation_type_check
     CHECK (operation_type IN ('addition', 'subtraktion', 'multiplikation', 'division', 'minigame_redeem'));
   ```

2. Erstelle `app/(child)/kind/spiel/actions.ts` — Server Action `startGameAction` per D-12, D-13:
   - "use server" Direktive
   - Import createClient aus @/lib/supabase/server
   - Import MINIGAME_THRESHOLD aus @/lib/config/rewards
   - Import revalidatePath aus next/cache
   - Funktion `startGameAction()` gibt `Promise<{ success: boolean; error?: string }>` zurueck
   - Auth-Check: `supabase.auth.getUser()` — return error wenn kein User
   - Punkt-Summe berechnen: `supabase.from("progress_entries").select("points_earned").eq("child_id", user.id)` dann `reduce` (bestehendes Pattern aus dashboard/page.tsx)
   - Race Condition Guard: Wenn totalPoints < MINIGAME_THRESHOLD → return `{ success: false, error: "Nicht genug Punkte." }`
   - Profil laden: `supabase.from("profiles").select("grade_level").eq("user_id", user.id).single()`
   - INSERT negative progress_entry:
     ```typescript
     await supabase.from("progress_entries").insert({
       child_id: user.id,
       operation_type: "minigame_redeem",
       grade: profile?.grade_level ?? 1,
       correct: true,
       points_earned: -MINIGAME_THRESHOLD,
     });
     ```
   - Bei INSERT-Fehler: return `{ success: false, error: "Fehler beim Speichern." }`
   - `revalidatePath("/kind/dashboard")` aufrufen (Pitfall 5 — Dashboard-Cache)
   - return `{ success: true }`

3. Erstelle `app/(child)/kind/spiel/page.tsx` — Server Component per D-16, D-18:
   - Import redirect aus next/navigation
   - Import createClient aus @/lib/supabase/server
   - Import MINIGAME_THRESHOLD aus @/lib/config/rewards
   - Import BalloonGame aus @/components/child/balloon-game (wird in Plan 02 erstellt — TypeScript wird temporaer einen Import-Fehler zeigen, der verschwindet wenn Plan 02 fertig ist)
   - Export default async function SpielPage()
   - Auth-Check: getUser() → redirect("/login") wenn kein User
   - Punkt-Summe berechnen (gleicher reduce-Pattern wie Dashboard)
   - Wenn totalPoints < MINIGAME_THRESHOLD → redirect("/kind/dashboard")
   - Return `<BalloonGame currentPoints={totalPoints} />`
   - Metadata: `export const metadata = { title: "Matheapp -- Ballonplatzen" }`
  </action>
  <acceptance_criteria>
    - Migration-Datei existiert: `test -f supabase/migrations/20260418000001_add_minigame_redeem_operation.sql`
    - Migration enthaelt minigame_redeem: `grep -q "minigame_redeem" supabase/migrations/20260418000001_add_minigame_redeem_operation.sql`
    - Server Action existiert: `test -f "app/(child)/kind/spiel/actions.ts"`
    - Server Action ist "use server": `grep -q '"use server"' "app/(child)/kind/spiel/actions.ts"`
    - Server Action exportiert startGameAction: `grep -q "export async function startGameAction" "app/(child)/kind/spiel/actions.ts"`
    - Server Action nutzt revalidatePath: `grep -q "revalidatePath" "app/(child)/kind/spiel/actions.ts"`
    - Server Action schreibt negative Punkte: `grep -q "\-MINIGAME_THRESHOLD" "app/(child)/kind/spiel/actions.ts"`
    - Spiel-Seite existiert: `test -f "app/(child)/kind/spiel/page.tsx"`
    - Spiel-Seite hat Server-Side Redirect: `grep -q 'redirect("/kind/dashboard")' "app/(child)/kind/spiel/page.tsx"`
    - Spiel-Seite importiert MINIGAME_THRESHOLD: `grep -q "MINIGAME_THRESHOLD" "app/(child)/kind/spiel/page.tsx"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "minigame_redeem" supabase/migrations/20260418000001_add_minigame_redeem_operation.sql && grep -q "export async function startGameAction" "app/(child)/kind/spiel/actions.ts" && grep -q 'redirect("/kind/dashboard")' "app/(child)/kind/spiel/page.tsx" && echo "PASS"</automated>
  </verify>
  <done>
    - Migration erweitert CHECK Constraint um 'minigame_redeem'
    - startGameAction prueft Auth + Punkte >= 500 + schreibt negative progress_entry (-500) + revalidatePath
    - /kind/spiel Server Component redirectet zu /kind/dashboard wenn Punkte unter Threshold
    - Spiel-Seite rendert BalloonGame mit currentPoints prop
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push --linked</name>
  <read_first>
    - supabase/migrations/20260418000001_add_minigame_redeem_operation.sql
  </read_first>
  <files>supabase/migrations/20260418000001_add_minigame_redeem_operation.sql</files>
  <action>
Fuehre die DB-Migration auf die verknuepfte Supabase-Instanz aus:

1. Stelle sicher dass SUPABASE_ACCESS_TOKEN in .env.local gesetzt ist
2. Fuehre aus: `npx supabase db push --linked`
3. Erwartetes Ergebnis: Migration 20260418000001_add_minigame_redeem_operation.sql wird erfolgreich angewandt
4. Verifiziere mit erneutem `npx supabase db push --linked` — sollte keine pending migrations mehr zeigen

Falls der Push interaktive Prompts zeigt die nicht unterdrueckt werden koennen, muss der Benutzer diese manuell bestaetigen.
  </action>
  <verify>
    <automated>npx supabase db push --linked 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Migration erfolgreich auf Supabase Cloud angewandt
    - progress_entries.operation_type akzeptiert jetzt 'minigame_redeem'
    - Keine pending migrations mehr vorhanden
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard-Integration — Spiel-starten-Button</name>
  <read_first>
    - app/(child)/kind/dashboard/page.tsx
    - lib/config/rewards.ts
    - components/child/dashboard-stats.tsx
  </read_first>
  <files>app/(child)/kind/dashboard/page.tsx</files>
  <action>
Erweitere `app/(child)/kind/dashboard/page.tsx` per D-11:

1. Import MINIGAME_THRESHOLD aus @/lib/config/rewards (explizit importieren fuer den konditionalen Button)
2. Nach dem bestehenden "Aufgaben starten"-Link und VOR dem `<div className="mt-auto">` Block:
   - Berechne `const gameUnlocked = totalPoints >= MINIGAME_THRESHOLD;`
   - Wenn gameUnlocked: rendere einen Link zu "/kind/spiel" mit dem Text "Spiel starten"
     ```tsx
     <Link
       href="/kind/spiel"
       className="h-16 flex items-center justify-center rounded-2xl bg-child-yellow text-slate-900 text-3xl font-semibold hover:opacity-90 focus:ring-4 focus:ring-child-yellow/50 focus:ring-offset-2 focus:outline-none"
     >
       Spiel starten
     </Link>
     ```
   - Wenn NICHT gameUnlocked: rendere einen deaktivierten div
     ```tsx
     <div className="h-16 flex items-center justify-center rounded-2xl bg-slate-200 text-slate-400 text-3xl font-semibold cursor-not-allowed">
       Spiel starten
     </div>
     ```

Wichtig: Den bestehenden Code (DashboardStats, ProgressBar, Aufgaben-Link, LogoutButton) NICHT veraendern. Nur den neuen Button zwischen dem "Aufgaben starten"-Link und dem mt-auto-div einfuegen.
  </action>
  <acceptance_criteria>
    - Dashboard enthaelt "Spiel starten" Text: `grep -q "Spiel starten" "app/(child)/kind/dashboard/page.tsx"`
    - Dashboard hat Link zu /kind/spiel: `grep -q '"/kind/spiel"' "app/(child)/kind/dashboard/page.tsx"`
    - Dashboard importiert MINIGAME_THRESHOLD: `grep -q "MINIGAME_THRESHOLD" "app/(child)/kind/dashboard/page.tsx"`
    - Dashboard hat deaktivierten Zustand: `grep -q "cursor-not-allowed" "app/(child)/kind/dashboard/page.tsx"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Spiel starten" "app/(child)/kind/dashboard/page.tsx" && grep -q '"/kind/spiel"' "app/(child)/kind/dashboard/page.tsx" && grep -q "MINIGAME_THRESHOLD" "app/(child)/kind/dashboard/page.tsx" && echo "PASS"</automated>
  </verify>
  <done>
    - Dashboard zeigt "Spiel starten" als aktiven Link (bg-child-yellow) wenn Punkte >= 500
    - Dashboard zeigt "Spiel starten" als deaktivierten Button (bg-slate-200, cursor-not-allowed) wenn Punkte < 500
    - Bestehender Dashboard-Content (Stats, ProgressBar, Aufgaben-Link, Logout) ist unveraendert
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server Action | Kind klickt "Spiel starten" — Punkt-Abzug muss server-seitig validiert sein |
| Browser -> /kind/spiel URL | Direkter URL-Zugriff ohne genuegend Punkte muss geblockt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | startGameAction | mitigate | Server Action prueft Punkte nochmals vor INSERT (Double-Check Pattern, Race Condition Guard) |
| T-40-02 | Elevation of Privilege | /kind/spiel page.tsx | mitigate | Server Component berechnet Punkt-Summe und redirectet wenn < 500 (D-16) |
| T-40-03 | Tampering | Doppel-Einloesung | accept | MVP-akzeptables Restrisiko — kein echtes Geld involviert. Fuer Produktion: DB-Level Lock |
| T-40-04 | Spoofing | startGameAction | mitigate | Auth-Check via getUser() — nur authentifizierte Kinder koennen einloesen |
</threat_model>

<verification>
- Migration-Datei enthaelt erweiterten CHECK Constraint mit 'minigame_redeem'
- supabase db push erfolgreich (Task 2 Checkpoint)
- Server Action startGameAction: Auth + Punkt-Check + negative INSERT + revalidatePath
- Spiel-Seite: Server-Side Redirect wenn Punkte < 500
- Dashboard: Konditionaler "Spiel starten"-Button
</verification>

<success_criteria>
- operation_type 'minigame_redeem' wird von der Datenbank akzeptiert
- startGameAction zieht 500 Punkte als negative progress_entry ab
- /kind/spiel ist nur erreichbar wenn Kind >= 500 Punkte hat
- Dashboard zeigt aktiven/inaktiven "Spiel starten"-Button je nach Punkt-Stand
</success_criteria>

<output>
After completion, create `.planning/phases/40-mini-game-reward/40-01-SUMMARY.md`
</output>
